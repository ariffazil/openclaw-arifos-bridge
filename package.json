{
  "name": "openclaw-arifos-bridge",
  "version": "1.0.0",
  "description": "Integration layer between OpenClaw gateway and arifOS constitutional kernel",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "healthcheck": "ts-node scripts/canon9_healthcheck.ts",
    "check:arifos": "curl -fsS http://127.0.0.1:8088/health",
    "test:integration": "npm run healthcheck && npm run check:arifos",
    "lint": "eslint src/ --ext .ts"
  },
  "dependencies": {
    "@openclaw/sdk": "^2026.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0",
    "eslint": "^8.57.0"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "license": "AGPL-3.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/ariffazil/openclaw-arifos-bridge"
  }
}
